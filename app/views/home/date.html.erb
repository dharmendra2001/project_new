
<%if @dates && user_signed_in? && current_user.user_type.upcase == 'ADMIN'%>

  <% @dates.each do |i|%>
  <% @a = User.find(i.user_id)%>
   <%= link_to @a.name, admin2_path(@a.id)%>
  <%end%>

<%end%>