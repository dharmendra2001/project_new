
  
  <% if user_signed_in? && current_user.user_type.upcase == 'ADMIN'%>
  <div class="col-lg-6 mx-lg-auto card shadow mt-5">
  
      <div class="card-header">
      <h3 class="card-title text-center">User Details</h3>
     </div>
    <div class="card-body">
    <p class="text-white bg-dark text-center"><%=flash[:error]%></p>
    <%= form_with model: @update_user_datas, method: :post, url: admin_update_path(@update_user_datas.id) do |form| %>
    
    
     <div class="form-group mb-2">
     <%= form.label :Name %><br>
      <%= form.text_field :name, class: "form-control form-control-lg"%>
    </div>

    <div class="form-group mb-2">
      <%= form.label :user_type %><br>
      <%= form.text_field :user_type , class: "form-control form-control-lg"%>
    </div >
    
    <div>
     <%= form.submit "update", class: "btn btn-primary btn-lg"  %>
    </div>
    <% end %>
    
    </div>
    </div>
  <%end%>